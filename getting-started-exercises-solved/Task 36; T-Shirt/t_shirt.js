"use strict";
/*Write a function called make_shirt() that accepts a size and the text of a message
that should be printed on the shirt.
The function should print a sentence summarizing the size of the shirt
and the message printed on it.
Call the function.*/
function make_shirt(size, statement) {
    console.log('Size of shirt should be', size);
    console.log("Text on shirt should be", statement);
}
make_shirt("Large", "The Boys");
